# AvionDash — DEPRECATED: Full VirtualHost Configuration
#
# This file was used when AvionDash was the ONLY site on the server.
# Since the application is now deployed as a subdirectory of an existing site,
# use aviondash-demo.conf instead.
#
# DO NOT place this file in /etc/httpd/conf.d/ — it will conflict with your
# existing site's VirtualHost configuration.
#
# See: aviondash-demo.conf  (Alias-based config for subdirectory deployment)
# See: SETUP.md Section 7   (full configuration instructions)
